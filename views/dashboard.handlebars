<div class="row align-center mb-5">
  <div class="col-md-6">
    {{#each posts as |post|}}
      <div>
          <h2><a href="/blogEdit/{{post.id}}>{{post.title}}</a></h2>
          <p>{{post.created_date}}</p>
          <p>{{post.content}}</p>
      </div>
    {{/each}}
  </div>
</div>